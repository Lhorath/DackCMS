# Copy to .env and adjust. Required for DB: DB_HOST, DB_USER, DB_NAME (DB_PASS may be empty).

APP_ENV=local

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=website
DB_PASS=dax123
DB_NAME=dackcms

CONTACT_FORM_TO=
